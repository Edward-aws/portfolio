version: 0.1

phases:
  build:
    commands:
      - npm instal
      - npm run webpack
artifacts:
      files:
        - index.html
        - favicon.ico
        - styles/main.css
        - images/*
        - dist/bundle.js
